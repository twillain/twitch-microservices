spring:
  application:
    name: discovery-service         # Nom du service
  cloud:
    config:
      uri: http://localhost:8888  # URL du Config Server
      profile: dev                # (Optionnel) Profil actif
      label: main                 # (Optionnel) Branche Git
