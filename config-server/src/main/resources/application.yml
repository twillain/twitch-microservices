server:
    port: 8888

spring:
    application:
        name: config-server
    cloud:
        config:
            server:
                git:
                    uri: https://github.com/twillain/Twitchbot-microservices/tree/main/config-repo
                    searchPaths:
                        - config-repo
                    clone-on-start: true